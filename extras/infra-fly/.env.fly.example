# Fly.io Configuration
# Run: ./scripts/init.sh <app-name> to set up
# Then edit this file with your values

# App names (set by init.sh)
FLY_APP_API=myapp-api
FLY_APP_WEB=myapp-web

# Region (https://fly.io/docs/reference/regions/)
FLY_REGION=nrt

# Database (see extras/infra-fly/postgres-vps.md for setup)
DATABASE_URL=postgresql://appuser:password@YOUR_VPS_IP:5432/appdb

# Backend secret key (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here
