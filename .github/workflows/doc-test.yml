name: Documentation Tests

on:
  push:
    paths:
      - "**.md"
      - "docs/**"
  pull_request:
    paths:
      - "**.md"
      - "docs/**"
  workflow_dispatch:  # Allow manual triggering
  schedule:
    # Run daily at 3 AM UTC (offset from repo-garden at 2 AM)
    - cron: "0 3 * * *"

jobs:
  markdown-lint:
    name: Markdown Lint
    runs-on: ubuntu-latest
    # Only run scheduled jobs on non-fork repos
    if: github.event_name != 'schedule' || github.event.repository.fork == false
    steps:
      - uses: actions/checkout@v4
      
      - name: Lint Markdown
        uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: "**/*.md"

  link-check:
    name: Link Validation
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule' || github.event.repository.fork == false
    steps:
      - uses: actions/checkout@v4
      
      - name: Check links
        uses: lycheeverse/lychee-action@v1
        with:
          args: --verbose --no-progress "**/*.md"
          fail: true

  api-doc-sync:
    name: API Documentation Sync
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule' || github.event.repository.fork == false
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        
      - name: Install dependencies
        run: cd backend && uv sync
        
      - name: Check API docs match routes
        run: |
          cd backend
          uv run python << 'EOF'
          import json
          from pathlib import Path
          
          # This is a placeholder - implement based on your actual API structure
          # Example: compare OpenAPI spec with documented endpoints
          
          print("API documentation sync check passed")
          EOF
